# 🏋️‍♂️ RE-FIT: 사용자 맞춤형 헬스장 큐레이션 서비스
> **공공데이터와 AI 로직을 결합한 스마트 피트니스 탐색 플랫폼**

본 프로젝트는 흩어져 있는 지역별 체육시설 데이터를 통합하고, 사용자의 위치와 취향에 맞는 최적의 운동 환경을 제안하기 위해 기획되었습니다. 단순한 정보 나열을 넘어, **데이터 정규화와 UX 최적화**를 통해 실질적인 편의성을 제공하는 데 집중했습니다.

---

## 🎯 1. 프로젝트 기획 의도
* **정보의 파편화 해결**: 여러 출처(CSV)로 나뉜 지역별 헬스장 정보를 하나로 통합하여 제공합니다.
* **사용자 중심 정렬**: 기계적인 가나다순이 아닌, 사용자가 인지하기 쉬운 '지점 번호' 중심의 논리적 정렬 알고리즘을 구현했습니다.
* **심리적 접근성 강화**: 찜 기능과 AI 기반의 루틴 추천을 통해 사용자가 운동을 시작하기까지의 심리적 문턱을 낮추고자 했습니다.

---

## ✨ 2. 핵심 기능

### 📍 지능형 헬스장 탐색 및 필터링
* **Multi-Source Data Merge**: 성동구, 용산구 등 서로 다른 공공데이터(CSV)를 실시간으로 병합하여 제공합니다.
* **URL 기반 상태 관리**: 검색어, 지역, 정렬 기준을 URL 파라미터와 동기화하여 페이지 새로고침이나 링크 공유 시에도 동일한 검색 결과가 유지됩니다.
* **전략적 이미지 폴백**: 고유 ID 기반의 Seed를 활용하여 랜덤하지만 사용자별로 고정된 시설 이미지를 제공하고, 로딩 실패 시 운동 기구 전용 이미지를 즉시 대체합니다.

### 💖 관심 목록 및 영속성 관리
* **Dual-Layer Persistence**: 찜한 시설의 'ID'와 '데이터 전체'를 이원화하여 관리하여, 원본 데이터셋이 업데이트되거나 사라져도 사용자의 찜 목록이 안정적으로 유지되도록 설계했습니다.

### 📞 스마트 문의 및 프로모션 시스템
* **동적 프로모션 빌더**: 시설별 이벤트 여부에 따라 혜택 텍스트를 실시간 생성합니다.
* **데이터 자동 바인딩**: 문의 접수 시 회원 정보를 자동으로 불러오고, 전화번호 정규화(Formatting)를 통해 오입력을 방지합니다.

---

## 🛠 3. 기술 스택

| 분류 | 기술 스택 | 이유 |
| :--- | :--- | :--- |
| **Frontend** | React 19 | 최신 Hooks 아키텍처를 통한 효율적인 상태 관리 |
| **State** | Context API, Redux Toolkit | 전역 유저 정보 및 대규모 데이터 상태 분기 관리 |
| **Routing** | React Router Dom | URL 파라미터를 활용한 선언적 라우팅 구현 |
| **Data** | PapaParse | 대용량 CSV 데이터의 클라이언트 사이드 고속 파싱 |
| **AI** | Ollama (Llama 3) | 개인화된 운동 루틴 및 식단 가이드 생성 엔진 |

---

## ⚡ 4. 성능 및 UX 최적화

### 🏎 연산 부하 제어 (Performance)
* **Debounce 알고리즘**: 검색어 입력 시마다 발생하는 빈번한 필터링 연산을 지연 처리하여 브라우저 부하를 줄였습니다.
* **Memoization**: `useMemo`와 `useCallback`을 적극 활용하여, 500개 이상의 아이템 중 실제 변경이 일어난 요소만 리렌더링되도록 최적화했습니다.
* **Intl.Collator 활용**: 단순 유니코드 비교가 아닌, 한국어 로케일에 최적화된 정렬을 구현하여 언어적 정확도를 확보했습니다.

### 🛡 데이터 무결성 (Data Integrity)
* **ID 정규화 (Normalization)**: 외부 소스마다 다른 ID 타입을 `String(id)`으로 강제 형변환하여 기능 오작동을 원천 봉쇄했습니다.
* **Encoding Defense**: `TextDecoder`를 통해 `UTF-8`과 `EUC-KR` 인코딩을 자동 감지하여 한글 깨짐 현상을 해결했습니다.

---

## 🧐 Technical Challenge & Resolution
> **가장 해결하기 힘들었던 부분: 데이터 불일치 대응**
> 
> 실제 공공데이터는 형식이 일정하지 않았습니다. 이미지가 없는 데이터에는 고유 락(Lock) 기반의 이미지를 배정하고, 인코딩이 깨진 데이터는 디코더로 복구하는 등의 처리를 거쳤습니다. 이는 단순 기능 구현을 넘어 실무 환경에서 마주할 수 있는 변수들을 직접 고민하고 해결한 소중한 경험이었습니다.
